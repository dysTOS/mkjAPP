import { NotVisibleForRolesDirective } from './not-visible-for-roles.directive';

describe('NotVisibleForRolesDirective', () => {
  it('should create an instance', () => {
    const directive = new NotVisibleForRolesDirective();
    expect(directive).toBeTruthy();
  });
});
